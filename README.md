# Fast react build with docker
This config is based on the one described [here](https://habr.com/ru/company/itsumma/blog/501680/)

Files to look at:
* Dockerfile
* .dockerignore
* nginx/static.conf

Every other file is from `npx create-react-app my-app --template typescript`
